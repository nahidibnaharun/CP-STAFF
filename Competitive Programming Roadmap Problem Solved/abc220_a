
using ll = long long;

/*
ID: mdnahidibnaharun
LANG: C++
TASK: practice
*/

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int a,b,c;cin>>a>>b>>c;
    int x=b/c*c;
    if(a<=x)cout<<x;
    else cout<<-1;
   return 0;
    
}
