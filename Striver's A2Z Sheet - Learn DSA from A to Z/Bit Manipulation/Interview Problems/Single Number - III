class Solution {
public:
    vector<int> singleNumber(vector<int>& nums) {
        int x=0;
        for(auto y:nums)x^=y;
        long long df = x&-(long long)x;
        int a=0,b=0;
        for(auto y:nums){
            if(y&df)a^=y;
            else b^=y;
        }
        return {a,b};
    }
};
