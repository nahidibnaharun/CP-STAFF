class Solution {
public:
    int maxDepth(string s) {
        int l = 0, mx = 0;
        for (auto& x : s) {
            if (x == '(')
                l++;
            else if (x == ')')
                l--;

            mx = max(mx, l);
        }
        return mx;
    }
};
