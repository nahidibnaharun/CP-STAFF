#include <bits/stdc++.h>

using namespace std;

int main()
{
   set <char> a;
   int n;
   string s;
  getline(cin,s);
   for(int i=0;i<s.length();i++){
       if(isalpha(s[i]))
       a.insert(s[i]);
       
   }
   cout<<a.size();
    return 0;
}
