#include <bits/stdc++.h>

using namespace std;

int main()
{
   char a[]="hello";
   string s;
   cin>>s;
   int cnt=0;
   for(int i=0,j=0;i<s.length();i++){
       if(s[i]==a[j]) {cnt++;
           j++;
       }
   }
   if(cnt>=5)cout<<"YES";
   else cout<<"NO";
    return 0;
}
