#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ull = unsigned long long;
using ld = long double;
using ui = unsigned int;
const int INF = 1e9 + 5;
const int MOD = 1000000007;
const int MAX = 10000;

/*
//      _   __  ___  __   __ _______ _______
//     / | / / / _ \ \ \__\ \\__  __\\  __  \
//    /  |/ / / /_\ \ \  ___ \  \ \   \ \ \  \
//   / /|  / / ____  \ \ \  \ \ _\ \___\ \/  /
//  /_/ |_/ /_/    \__\ \_\  \_\\______\\___/
//
// ID: mdnahidibnaharun
// LANG: C++
// TASK: practice
*/

void solve() {
    ll t;
    cin >> t;
    string str, str1(10, '0');

    for (ll i = 0; i < t; i++) {
        char ch;
        cin >> ch;
        str += ch;
    }

    for (ll i = 0; i < t; i++) {
        if (str[i] == 'L') {
            for (ll j = 0; j < 10; j++) {
                if (str1[j] == '0') {
                    str1[j] = '1';
                    break;
                }
            }
        } else if (str[i] == 'R') {
            for (ll j = 9; j >= 0; j--) {
                if (str1[j] == '0') {
                    str1[j] = '1';
                    break;
                }
            }
        } else {
            ll k = str[i] - '0';
            str1[k] = '0';
        }
    }

    cout << str1 << endl;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    solve();
    return 0;
}
