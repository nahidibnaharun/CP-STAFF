#include <bits/stdc++.h>

using namespace std;

using ll = long long;

/*
ID: mdnahidibnaharun
LANG: C++
TASK: practice
*/

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int a,b;cin>>a>>b;
    if(a==b+1 || b==a+1 || abs(a-b)==9)cout<<"Yes";
    else cout<<"No";

   return 0;
    
}
